/**
 * descriptors.ts — DEPRECATED static registry
 *
 * Descriptor files are now loaded lazily via loadDescriptors() in IconContext.tsx.
 * This file is kept as an empty stub to avoid breaking any future imports.
 *
 * To regenerate descriptor files:
 *   npm run generate-icons
 */

export type { IconDescriptor } from '../types/icon';
